# Copy to .env (gitignored) or export before running.
# Free NASA key (instant): https://api.nasa.gov/  ·  DEMO_KEY works but is heavily rate-limited.
NASA_API_KEY=DEMO_KEY

# --- Production profile (SPRING_PROFILES_ACTIVE=prod) only ---
# DATABASE_URL=jdbc:postgresql://HOST/DB?sslmode=require
# DATABASE_USERNAME=
# DATABASE_PASSWORD=
# CORS_ORIGINS=https://your-app.vercel.app
